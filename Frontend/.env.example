# Base URL of the running Royal Care CMS backend.
# Local dev default is the Express server on port 4000.
# In production, set this to your deployed CMS URL (e.g. https://cms.royalcarefoundation.com).
VITE_CMS_URL=http://localhost:4000
